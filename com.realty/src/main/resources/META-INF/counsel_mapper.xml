<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="repository.CounselRepository">

<insert id="saveCounsel" parameterType="dto.SaveCounselRequestDTO">
	insert into counsel(m_name, m_email, m_tel, c_rental, c_type, counselDate, c_content)
				values(#{m_name}, #{m_email}, #{m_tel}, #{c_rental},#{c_type},#{counselDate},#{c_content})

</insert>

</mapper>